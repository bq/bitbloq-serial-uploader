/* global define */
define(function() {
    'use strict';

    /* return an array of specs to be run */
    return {
        specs: [
            'spec/program',
            'spec/serial'
        ]
    };
});
